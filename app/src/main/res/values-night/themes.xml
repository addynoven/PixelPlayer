<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.NeonPlayer" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Splash screen theme can still extend Theme.SplashScreen, but ensure it transitions to the correct base theme -->
    <!-- For dark mode, postSplashScreenTheme should ideally point to a dark version of Theme.NeonPlayer if specific overrides are needed -->
    <!-- However, since Theme.NeonPlayer itself will be resolved from values-night, this setup should work. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FEFEFE</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.NeonPlayer</item>
    </style>
</resources>
